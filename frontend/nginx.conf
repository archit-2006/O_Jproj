server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line fixes the 404 errors on page refresh
        try_files $uri $uri/ /index.html; 
    }
}